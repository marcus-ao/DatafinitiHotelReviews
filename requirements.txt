# ============================================================
# Hotel Reviews Knowledge Base - Python dependencies
# Install: pip install -r requirements.txt
# Python: 3.10+
# ============================================================

# Data processing
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0
pyyaml>=6.0
scikit-learn>=1.3.0

# NLP - sentence splitting
spacy>=3.7.0
# After install: python -m spacy download en_core_web_sm

# NLP - aspect classification and sentiment
transformers>=4.40.0
torch>=2.1.0
# For CUDA builds, install torch from https://pytorch.org

# Embeddings
sentence-transformers>=2.7.0

# Vector database
chromadb>=0.5.0

# PostgreSQL
psycopg2-binary>=2.9.9

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
openai>=1.30.0
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0
seaborn>=0.13.0
# Needed when reading some pandas pickle artifacts generated in Colab / Arrow-backed environments
pyarrow>=23.0.1

# E10 / PEFT training
accelerate>=0.30.0
datasets>=2.20.0
peft>=0.11.0
trl>=0.9.0
# Linux GPU only. The environment marker avoids breaking local non-Linux installs.
bitsandbytes>=0.43.0; platform_system == "Linux"

# Optional inference accelerator
# optimum>=1.19.0        # ONNX acceleration
